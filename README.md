# NEXTJS SCAFF HARNESS

This folder is the canonical pointer for `/gsd-new-project` and follow-on GSD planning.

Primary file:

- `.plan/SOURCE_OF_TRUTH.json`

Init Phase:
1. /init or /init-p the project.
2. git clone https://github.com/ktg-one/mcp-webdev
3. run fastmcp and ensure you are connected.
4. 


Execution rule:

1. Complete Init phase first (`app-spec-agent` then `Init-agent`).
2. Persist outputs in `docs/plans/`.
3. Treat `docs/plans/2026-04-04-swap-features.json` as the primary replacement manifest.
4. Start GSD planning only after init artifacts are in place.
- GSD: Code Map,  new project, Research project, NEW PHASE, Research phase, list phase assumptions, plan phase, execute phase, create milestone , this milestone expectations , List milestone assumptions, autonomous, etc.I 
**you can only execute when the entire plan is done **
Proof of architecture Clothes on it build the site in 1. shot and it was because of the plan it SAID.
5. If assets are missing, stay in planning-only mode.
